# HTTP Server Library
# Go to project folder

Windows (cmd):

```bash
cd ./tasks/term-2/1
## Compilation

```bash
# Compile all files
javac -d . com/httpserver/*.java Main.java
# Run the server
java Main
