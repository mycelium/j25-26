## Quick Start

### 1. Clone and build

```powershell
git clone <repo-url>
cd json-parser
.\gradlew.bat build
```

### 2. Run the demo server

```powershell
.\gradlew.bat run
```
### OR build JAR
```powershell
.\gradlew.bat clean jar
java -jar build\libs\json-parser-1.0-SNAPSHOT.jar
```
