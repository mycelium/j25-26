# JSON Parser in Java

A JSON parsing library written in Java with no external dependencies.  
Supports parsing JSON strings into Java objects and converting Java objects back to JSON.

# Build & Run

## Go to project folder

Windows (cmd):

```bash
cd ./tasks/term-2/1

## Compile all files

javac jsonlib\*.java Main.java User.java

## Run program
java Main
```
