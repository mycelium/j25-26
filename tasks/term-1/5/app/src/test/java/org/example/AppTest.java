package org.example;

import org.junit.jupiter.api.Test;

class AppTest {
    @Test
    void testAppDoesNotCrash() {
        // Этот тест ничего не проверяет, но позволяет Gradle успешно собрать проект.
        // Он нужен, чтобы папка `test` не была пустой и сборка не падала.
    }
}