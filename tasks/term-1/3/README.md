## Ответ на вопрос: подумать где хранятся элементы множества?
Элементы не хранятся явно в памяти. Множество представлено как функция-предикат (Int => Boolean), которая динамически вычисляет принадлежность элемента (возвращает True, если принадлежит и False, если не принадлежит).
